apiVersion: v1
kind: Service
metadata:
  name: processing
spec:
  selector:
    app: processing
  ports:
    - protocol: TCP
      port: 8585
      targetPort: 8585
      nodePort: 30585
  type: NodePort
